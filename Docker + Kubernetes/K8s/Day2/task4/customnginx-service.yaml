apiVersion: v1
kind: Service
metadata:
  name: customnginx-service
spec:
  type: NodePort                
  selector:
    app: customnginx
  ports:
  - port: 80                    
    targetPort: 80              
    nodePort: 30080             
